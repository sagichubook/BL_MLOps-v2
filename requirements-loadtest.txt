# Locust pins `requests<2.32.5`, which conflicts with tabpfn-client's
# `requests>=2.32.5` — the load generator never imports tabpfn-client (it
# only speaks HTTP to the running API), so it gets its own environment
# instead of sharing one with the app.
locust==2.42.5
